apiVersion: v1
kind: Service
metadata:
  labels:
    app: mogul-mvp-be-marketplace
  name: mogul-mvp-be-marketplace-svc
  namespace: mogul-dev
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 5001
  selector:
    app: mogul-mvp-be-marketplace
